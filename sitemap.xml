---
layout: null
sitemap: false
---
{%- comment -%}
自己写的 sitemap，覆盖 jekyll-sitemap 插件（源码根目录存在 sitemap.xml 时插件会让位）。

为什么不能用插件：教学笔记是定时发布的，未到日期的文章被 future:false 挡住不生成页面，
但它们仍留在 site.channel 集合里，插件照样会把 URL 写进 sitemap，结果是一堆指向 404 的地址。
这里按 date <= site.time 过滤，只收录真正已经生成出来的页面。

sitemap: false 的页面一律排除（index.html 重定向壳、Member 登录墙、百度验证文件，
以及与 No.001 主题重叠、故意不参与关键词竞争的两篇笔记）。
{%- endcomment -%}
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
{%- for page in site.html_pages -%}
{%- if page.sitemap != false and page.url -%}
  <url>
    <loc>{{ page.url | replace: '/index.html', '/' | absolute_url | xml_escape }}</loc>
{%- if page.last_modified_at or page.date %}
    <lastmod>{{ page.last_modified_at | default: page.date | date_to_xmlschema }}</lastmod>
{%- endif %}
  </url>
{%- endif -%}
{%- endfor -%}
{%- for doc in site.channel -%}
{%- if doc.sitemap != false and doc.date <= site.time -%}
  <url>
    <loc>{{ doc.url | absolute_url | xml_escape }}</loc>
    <lastmod>{{ doc.last_modified_at | default: doc.date | date_to_xmlschema }}</lastmod>
  </url>
{%- endif -%}
{%- endfor %}
</urlset>
